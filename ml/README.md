# Machine Learning Workspace

Use `src/` for reusable data, training, calibration, evaluation, and inference
modules. Keep notebooks exploratory and ensure experiments can be reproduced
from versioned configuration files.

