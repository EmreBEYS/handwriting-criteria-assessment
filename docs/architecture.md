# Architecture Decisions

Record the selected mobile framework, inference location (device or service),
model format, API framework, storage policy, authentication needs, supported
devices, latency target, offline requirements, and model-update strategy here.

