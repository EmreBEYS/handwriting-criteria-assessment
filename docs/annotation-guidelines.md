# Annotation Guidelines

This guide should include representative positive, negative, and ambiguous
examples for every approved criterion. Define the annotation interface, reviewer
training, disagreement-resolution process, quality checks, and inter-annotator
agreement measure before producing the main dataset.

